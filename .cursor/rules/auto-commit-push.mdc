---
description: Commit and push to origin after completing work in this repo (user preference)
alwaysApply: true
---

# Auto commit and push

The user wants changes deployed to the live site without being asked. After you finish a task that changes project files, **commit and push automatically** — do not wait for a separate "push" request.

## When to commit and push

- There are changes to tracked project files (source, config, docs).
- The task is complete or a coherent slice of work is done (not mid-edit).

Skip when:

- There are no file changes.
- Only generated artifacts changed (`node_modules/`, `dist/`) — these must stay gitignored.
- Files likely contain secrets (`.env`, credentials).

## Workflow

1. Run `git status` and `git diff` to see what changed.
2. Stage relevant files only (never secrets).
3. Write a concise 1–2 sentence commit message focused on **why**.
4. Commit on the current branch.
5. **Immediately run `git push`** (`git push -u origin <branch>` if upstream is not set).

## Git safety

- Never update git config, skip hooks, or force-push unless the user explicitly requests it.
- Do not amend unless the user asked or a hook auto-modified files after your commit.
- On Windows, `gh` may require the full path: `"C:\Program Files\GitHub CLI\gh.exe"`.

## User communication

- Briefly note the commit hash/message and that the push succeeded (include the GitHub repo URL if known).
- If push fails (auth, no remote), report the error and what the user should do; still commit locally when appropriate and say push is pending.
